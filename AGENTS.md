---
tense: 'living'
describes: '项目执行入口'
status: 'current'
shaped-by: ['016', '002', '003', '014', '015']
---

# 项目执行入口

按任务取用资料：功能行为从 `docs/features/README.md` 定位，重要规则变更查 `.specify/memory/constitution.md`，检查与发布查 `docs/system/checks-and-release.md`。已知路径的小修正直接读取相关文件；本轮已读取且未变化的材料不重复读。

## 找东西的规则

- 现在的功能行为 → docs/features/；数据、配置、接口和交付见其链接的docs/system/说明。
- 当初为什么这么定 → 从功能文档shaped-by到specs/NNN-*/，技术理由看research.md。
- 谁在什么时候改了什么 → git log / PR，不写文档流水账。
- 现在该做什么 → specs/README.md中status为in-progress的条目。

优先沿索引定位；发现缺失或错误链接时修正，不限制正常排查的读取次数。仓库地图见docs/README.md；产品方向见docs/PROJECT_ANALYSIS.md。

## 协作与范围

对用户的命令、建议和参考资料先独立判断适用性、收益与代价；主动报告发现的实际问题、冲突及更好方案，说明影响、优先级和取舍，不等用户逐项追问。用户不一定掌握技术细节，不能默认建议正确，也不为反驳而反驳。

用户不会写代码：用中文和外行能理解的解释；小任务动手前简短说明，复杂任务用3–5句话，交付说明文件与验证方法。每轮最多3个问题，已确认的不重复问。新需求从用户输入与现有上下文明确用户、路径、明确不做与边界；范围明确且已授权时持续执行至实现、必要验证和文档交付完成。只有缺少影响范围、成本、权限或正确性的必要决定时才问；独立问题每轮合并最多3个，依赖问题逐个问，回答不限制词数。用户要求阶段审阅或只做某阶段时按约定暂停，已给出的授权不重复确认。

当前只做Explore，站内不开放公众或Agent编辑及Markdown投稿，文章改进通过GitHub PR审阅，保留现有UI。Astro+TypeScript、Cloudflare优先；复杂业务按需讨论，不提前安装React/Tailwind或建立空服务。新增npm依赖先说明必要性并获同意；Pagefind已按许可安装为构建依赖。

## 执行与文档

新功能、跨模块改造和重要规则变更使用Spec Kit；小修复、文档修正直接执行并说明范围及验证，不强制新建规格或走全部阶段。已有规格覆盖的调整原地完善未冻结稿。research记录技术未知或重要取舍；附加产物由Agent按必要性在spec列明用途，不逐文件请示，也不因上游示例自动生成。项目Skills由 `.specify/presets/vibes/` 命令源生成，阶段推进由当前对话负责；不启动原生完整workflow，避免重复派发和丢失必要问题的暂停状态；使用和更新见docs/system/agent-guidance.md，不直接编辑生成文件。

功能说明以用户完成一件事的操作路径为单位，按docs/features/_TEMPLATE.md写操作、文件、验收、测试、依赖和已知问题；访客与维护者分组，手机/语言/异常并入路径。需要解释系统响应时画简短流程图，并核对真实代码顺序。归并文档用legacy-feature-ids保留历史编号的唯一对应，不改旧规格正文。

验收复选框表示最近一次仍有效的证据，记录范围、日期和方法；文档排版或新对话不清空已通过项。相关代码使结论失效时重新验证，未覆盖项明确保留。spec全部任务完成后同步complete与索引，合并状态看Git，不在文档中重复手抄。

living只写当前行为；spec开发中可改，合并后冻结，仅允许状态、后续修订关系和首次据实补记日期。改已有功能时新旧spec互相引用，功能文档shaped-by可追溯。交付需同步受影响功能文档及索引，不要求固定任务位置或措辞，合并提交让代码与说明同时生效，合并后核对，不另拖补文档PR。

临时稿放.scratch且不提交，原始本地证据放resources/evidence；代码变更流水账留PR/git。不要在根目录堆报告，或在living中保留“本次改了什么”。上游工具资产和产品文章按各自格式管理，不改成治理文档。

## 检查与交付

- 自有代码按清晰职责拆分；300行仅审阅提示，注释解释不明显的原因、约束与复杂逻辑，不重复清楚的函数名，复用已有工具。
- 按影响选择检查：纯治理文档运行docs:check与format:check；工具代码运行check；正文及受限MDX内容按content通道执行check、构建、预算与文章冒烟，组件/路由/构建/测试基础设施运行verify与budget。纯Agent指导文件同样走轻量文档检查，混入网页、脚本、依赖或GitHub CI配置时按实际影响升级。无法判定影响时完整验证；必需检查失败不得跳过。失败只暂停依赖步骤，修复本任务造成的问题并重跑受影响检查；只有缺少必要信息、权限或外部条件时才等待用户。main发布只有取得与最终完整文件树一致的可信PR验收证明时可复用回归结论，仍运行本次生产构建、基础检查、预算、产物及线上核验；无证明或有冲突自动完整回退。范围分类见docs/system/checks-and-release.md。
- 自动化E2E本地与CI统一Playwright；涉及布局、交互体验时按需用内置浏览器探索或视觉审阅，不维护第二套自动化脚本。数据库命令仅本地，不接受线上参数。
- 功能文档120行、宪章100行、AGENTS150行仅作篇幅提示；spec目录无总行数上限。依据重复、过期、职责和导航审阅，不为凑数拆分或删信息。
- 代码是给机器执行的，Markdown是给用户阅读的同一实现说明；配置、数据、接口、规则与交付同步docs/system对应文档。code-sources/code-revision记录源码对应，修改源码后先复核文字、路径和验收，再更新摘要，不盲目刷新。
- 仅需要Spec Kit的需求必须使用分支和PR，按完整需求组织，首版spec建立时创建Draft PR；同一需求复用未合并分支和已有PR。小修复、文档和小型规则补充直接在当前工作分支提交，不单独开PR。小的措辞和可选日期补记可随相关工作提交，不单独催用户合并；当前行为、源码对应与实现完成状态必须随同一批代码更新，不能延后；不夹带无关功能或无限扩大PR。具体收尾见docs/system/checks-and-release.md。
- commit用于保存进度，可有多次；推送用于备份或运行CI，不等于提PR、合并或发布。需要Spec Kit的需求不直接推main，用户明确指令前不合并PR；小改动验证后可直接提交推送，远端保护阻止时保留进度并说明，不擅自更改保护或另开PR；用户已授权vibes.college为正式发布目标，main检查通过后自动部署，上线验收后清理本任务资源。
- 如冻结规则与需求冲突，新增spec，不修改旧正文或削弱检查。真实失败、未测和未部署必须明确报告。

## PR工作台与经验转化

首版spec确认目标后开Draft PR；描述维护目标、范围、任务摘要、当前进度、阻塞、下一步和预览链接/状态，评论只记录重要决定、阶段证据与阻塞变化。详细任务以tasks.md为准，摘要随阶段同步。用户能打开PR看清单是交付条件，不只报告“已创建”。

本地commit可多次；达到可体验阶段、交接、暂停或结束前保存并推送有效进度，更新PR。不硬性规定每commit push或本地最多领先一个commit。开发中阶段预览由AI按需运行release:preview，不每次push重建。基础设施或重要PR在Ready前，主Agent主动新建独立会话，由另一Agent审查整个PR，按实际差异与风险确定重点，覆盖行为正确性、安全与数据边界、性能与资源、代码可维护性、验证与交付；具体标准见docs/system/checks-and-release.md，修复后由独立审查者复核最终SHA。独立审查通过才转Ready，必要检查通过后通知用户点合并，AI不自动合并。Ready后再改代码须退回Draft、复核变化并重新验收整个PR；具体范围与记录见docs/system/checks-and-release.md。

AI负责真实.dev.vars与线上环境值配置；缺信息问用户，需要本人登录或授权就给入口、步骤、完成标志；秘密不得进入Git/PR/日志。不要要求用户手写配置。

每个PR合并后依据PR最终差异和合并后代码，核对所有受影响的docs说明无遗漏、冲突或过时描述；最终差异不足以解释变更时才追查具体commit。被撤销或替代的行为以最终实现为准，无文档影响不凑写内容。核对结果和待补项记在PR收尾评论，不能仅凭摘要或自动检查通过认定一致。

开始相关任务时读docs/LESSONS.md；满足记录条件才添加三行经验，最多30条。转化可直达规则、模板/PR清单或自动检查，补措施与验证证据；已转化满30天后复核有效性再删除。原则理由在docs/DECISIONS.md保留，不混入活任务。

PR合并且必要检查、上线验收通过后，Agent同步空闲的本地主目录：没有本地独有提交时快进，有本地提交时保留并合并origin/main，处理冲突后按影响验证，不以存在本地提交为由停止同步；并按cleanup:task清理已合并的无用分支和worktree；保留未保存工作、其他任务与回滚资源。
