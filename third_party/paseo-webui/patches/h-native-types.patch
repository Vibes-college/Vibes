diff --git a/packages/app/index.ts b/packages/app/index.ts
index 1f56319..4b060a4 100644
--- a/packages/app/index.ts
+++ b/packages/app/index.ts
@@ -33,7 +33,10 @@ const handle: PaseoHandle = {
     if (command.type === "dispose") {
       disposed = true;
       options?.onEvent({ version: 1, type: "state", value: "disposed" });
-      if (mounted) AppRegistry.unmountApplicationComponentAtRootTag(mounted);
+      // React Native declares numeric tags; the verified react-native-web export
+      // delegates this call to unmountComponentAtNode and requires the DOM root.
+      if (mounted) (AppRegistry.unmountApplicationComponentAtRootTag as unknown as
+        (root: HTMLElement) => void)(mounted);
       // A document exit releases the singleton runtime and native module listeners
       // without editing its sync/cache internals or erasing the saved host registry.
       window.location.reload();
diff --git a/packages/app/src/embedded/public-work-reference.test.tsx b/packages/app/src/embedded/public-work-reference.test.tsx
index b3fe065..0c34fae 100644
--- a/packages/app/src/embedded/public-work-reference.test.tsx
+++ b/packages/app/src/embedded/public-work-reference.test.tsx
@@ -21,7 +21,7 @@ let root: Root;
 let host: EmbeddedHost;
 let dom: JSDOM;
 let listeners: Set<() => void>;
-let change: ReturnType<typeof vi.fn>;
+let change = vi.fn<(text: string) => void>();
 let latest = '';
 function Harness({ locked = false, initial = '请解释' }: { locked?: boolean; initial?: string }) {
   const [value, setValue] = useState(initial);
@@ -77,7 +77,7 @@ beforeEach(() => {
     onEvent() {},
   };
   vi.stubGlobal('__vibesPaseoEnvironment', host);
-  change = vi.fn();
+  change = vi.fn<(text: string) => void>();
   root = createRoot(host.root);
 });
 afterEach(() => {
diff --git a/packages/app/tsconfig.json b/packages/app/tsconfig.json
index 1098a15..b2b8113 100644
--- a/packages/app/tsconfig.json
+++ b/packages/app/tsconfig.json
@@ -3,8 +3,11 @@
   "compilerOptions": {
     "strict": true,
     "paths": {
-      "@/*": ["./src/*"]
-    }
+      "@/*": [
+        "./src/*"
+      ]
+    },
+    "allowImportingTsExtensions": true
   },
   "include": [
     "**/*.ts",
