---
tense: 'frozen'
describes: '规模化串联与原作接入的具体取舍'
status: 'in-progress'
amended-by: []
---

# 已确认的技术取舍

## 目录与事实

上游固定版本`eda1b85ed81ab45d0f0cbc27dc0206560d11c801`的registry有48条、7类；源码目录有52个文件，文件数不是作品数。目录与预览源码路径已交叉核对；逐件行为须继续看实现与真实页面。Twitter与GitHub卡片另有外部数据服务依赖，不能以静态元数据声称请求成功。

## 串联的计算边界

采用用户路径的角色查询和有界候选扩展，规则绑定能力、作用域与时段；只记录有实际依据的具体作品组合和例外。6步、每步8项、每轮6份的单模板扩展至多约288次，仍需计算整份约束，不代表总耗时或全局最优。没有合格方案时给出缺口、扩大相关候选或省略可选效果。

48件先用本地结构化记录，无需向量数据库。千件后的索引与语义召回按实际测量增加；合成负载不能证明真实推荐质量。

## 事件交接与真实效果

错峰转场的`onViewSwap`在遮挡到位后调用，原组件没有对外的退出完成事件；50ms后开始退出，不能保证异步页面就绪。实际路径需要明确覆盖、内容就绪和退出完成，并验证重复点击和失败后的恢复。

文字揭示的`containerRef`可指定滚动容器，但没有现成`enabled`属性；延迟启用需要适配。问答默认第二项、单项内部状态；多项比较需要改变状态结构并重新核对规则，不能只改文案。

保留原作录屏作为参考；真实路径是带具体适配的应用页面，单独标明已实现的变化与验证范围，避免把参考录屏当作组合运行。

## 来源与许可

[固定LICENSE](https://github.com/Saurabh-2607/GreatUI/blob/eda1b85ed81ab45d0f0cbc27dc0206560d11c801/LICENSE)允许组件用于个人与商业应用，限制作为UI套件、模板或组件库再分发；README与源码中的MIT标注不一致。全量内容保留原创解释和来源链接，少量路径实现保留准确许可与修改记录，不制作整库代码复制产品。

素材URL可用不代表授权再发布；当前本地试用按实际来源加载，受限URL不重试规避、不换代理。已有三段网页补录继续标明来源和未公开的素材边界。

## 工作目录与发布

原017分支的文章接入未合并且有旧改动，本需求从origin/main隔离实施018。本地独立入口可以复用项目依赖和预览端口，保持生产路由不变。当前没有发布授权，不触发部署流程。
